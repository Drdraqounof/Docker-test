// next.config.ts
// Next.js configuration file for custom build and output settings.
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "standalone",
  /* config options here */
};

export default nextConfig;